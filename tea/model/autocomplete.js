/**
 * Autocomplete Model
 *
 */
define(
   [ 'lib/backbone' ],
   function( Backbone )
   {
      var autocomplete = Backbone.Model.extend({
         initialize : function()
         {
         }
      });

      return {};
   }
);
